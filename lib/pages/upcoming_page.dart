import 'package:flutter/cupertino.dart';

class UpcomingPage extends StatelessWidget {
  final Key key;

  UpcomingPage(this.key) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('upcoming'),
    );
  }
}
